#include "main.h"
/**
 * _isdigit - function that checks if c is upper
 * @c: parameter to be checked
 * Return: 1 if upercase
 * and 0 otherwise
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
return (1);
else
return (0);
}
